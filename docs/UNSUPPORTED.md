# Unsupported commands

Commands are omitted when IntelliJ has no meaningful equivalent or when a mapping would be misleading. The machine-readable initial list lives in `mappings/unsupported.yml`.

This document will become exhaustive once the canonical VS Code default JSON snapshots are added and every command is audited.
